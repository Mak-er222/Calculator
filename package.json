{
  "name": "calculator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist/css dist/js && cp templates/calculator.html dist/index.html && cp src/css/* dist/css/ && cp src/js/* dist/js/",
    "predeploy": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "express": "^5.1.0",
    "shx": "^0.4.0"
  }
}
